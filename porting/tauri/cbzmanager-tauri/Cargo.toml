[package]
name = "cbzmanager-tauri"
version.workspace = true
edition.workspace = true
authors.workspace = true

# Placeholder - full Tauri v2 setup in Checkpoint 6
# Requires: cargo install tauri-cli, tauri.conf.json, web/ frontend
[lib]
name = "cbzmanager_tauri"
path = "src/lib.rs"

[[bin]]
name = "cbzmanager"
path = "src/main.rs"

[dependencies]
rust-core = { path = "../rust-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
# tauri build placeholder - to be configured in Checkpoint 6
